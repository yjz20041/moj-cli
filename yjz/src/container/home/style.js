import {
    StyleSheet,
} from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: 'grey',
        position: 'relative',
    },

    btn: {
        marginTop: 5,
        color: 'red',
    }
});
